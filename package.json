{
  "name": "perlego-curiosity-onboarding",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "node scripts/build.mjs",
    "test": "node --test src/**/*.test.js",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
